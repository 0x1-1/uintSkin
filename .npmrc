# Mirrors are set elsewhere to avoid npm "Unknown config" warnings during builds.
# If you need to override download sources, set environment variables before running:
#   ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
#   ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/
